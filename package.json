{
  "name": "folder-recipe-switcher",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --config site/vite.config.js",
    "preview": "vite preview --config site/vite.config.js --host 127.0.0.1",
    "test": "cargo test --all-targets && npm run build:site && node --test site/tests/site.test.mjs && npm run test:claims && playwright test",
    "test:claims": "node scripts/test-claims.mjs",
    "build:site": "vite build --config site/vite.config.js && node scripts/build-site.mjs",
    "build": "cargo build --release && npm run build:site && node scripts/copy-binary.mjs"
  },
  "devDependencies": {
    "@axe-core/playwright": "4.10.2",
    "@playwright/test": "1.58.2",
    "vite": "6.4.3"
  }
}
