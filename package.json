{
  "name": "folder-recipe-switcher",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --config site/vite.config.js",
    "preview": "vite preview --config site/vite.config.js --host 127.0.0.1",
    "test": "cargo test --all-targets && node --test site/tests/*.test.mjs && npm run build:site && playwright test",
    "build:site": "vite build --config site/vite.config.js",
    "build": "cargo build --release && npm run build:site && node scripts/copy-binary.mjs"
  },
  "devDependencies": {
    "@axe-core/playwright": "4.10.2",
    "@playwright/test": "1.58.2",
    "vite": "6.4.3"
  }
}
