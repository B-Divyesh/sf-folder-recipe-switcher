# Blue-hour archive sample

This bundled sample represents two small photography jobs: window-light portraits and scanned family negatives. The `.RAF`, `.jpg`, and `.tiff` files are harmless text fixtures with realistic names, not photographs.

Run `folder-recipe demo` to copy them into a new temporary folder, create two recipe files, inspect an inherited recipe, and export an import checklist.
